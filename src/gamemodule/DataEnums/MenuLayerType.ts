export enum MenuLayerType {
    // 主界面
    Home = 1,

    // 主界面 上一层
    HomeTop = 2,

    // 模块
    Module = 3,

    // 主UI
    MainUI = 4,

    // 对话框
    Dialog = 5,

    // 引导
    Guide = 6,

    // 浮动对话框
    FloatMsg = 7,

    // 加载面板
    Loader = 8,
}